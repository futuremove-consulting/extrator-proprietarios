# As dependencias sao gerenciadas no pyproject.toml (fonte unica).
# Instalacao (dev incluido): pip install -e ".[dev]"
#
# 2026-09 (sanitation pass): pydantic, typer, rich, fastapi, uvicorn e
# python-dotenv foram removidas do pyproject - zero imports no codigo.
# A API REST futura deve re-adicionar fastapi/uvicorn ao implementa-la.
#
# Integracoes futuras (quando implementar, adicionar ao pyproject):
#   PostgreSQL/Supabase: psycopg2-binary, sqlalchemy
#   Browser automation: playwright, selenium
